server:
  port: 9296
spring:
  application:
    name: CONFIG-SERVER
  cloud:
    config:
      server:
        git:
          uri: https://github.com/rhounkpe/microservices-using-spring-boot-config-server-config-repo
          clone-on-start: true
          default-label: main
